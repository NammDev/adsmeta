import SkeletonSecondaryPage from '@/components/loading/skeleton-secondary-page'

export default function Loading() {
  // We're using the secondary page skeleton for all pages now
  return <SkeletonSecondaryPage />
}
